<!DOCTYPE html>
<html>

<head>
    <title>BREADS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-u1OknCvxWvY5kfmNBILK2hRnQC3Pr17a+RTT6rIHI7NnikvbZlHgTPOOmMi466C8"
        crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/fontawesome/css/all.css">
    <link rel="stylesheet" href="/stylesheets/bootstrap.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="/javascripts/jquery-3.6.1.js"></script>
    <link rel="shortcut icon" href="#">

  
</head>

<body>
    <li><a href="/Logout"><span class="glyphicon glyphicon-user"></span>Logout</a></li>

    <div class="container">
        <div class="card-header">
            <h1>Barang</h1>
            <table class="table table-striped">
                <thead id="sorting">
                    <tr>
                        <th>id_barang</th>
                        <th>nama_barang</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>

            <div class="row">
                <div class="col-6">
                    <ul aria-label="Page navigation" id="pagination">
                    </ul>
                </div>
                <div class="col-6">
                    <div class="row">

                    </div>
                </div>

            </div>
            <form id="form-tambah">
                <input type="hidden" id="id" name="id" value="">
                <div class="card">
                    <div class="card-header">
                        <h2>Form Barang Tambah</h2>
                    </div>

                    <div class="card-body">

                        <div class="row mb-3">
                            <label for="id_barang" class="col-sm-2 col-form-label">id_barang</label>
                            <div class="col-sm-10">
                                <input type="text" class="form-control" id="id_barang" name="ID_barang" required>
                            </div>
                        </div>
                        <div class="row mb-3">
                            <label for="nama_barangs" class="col-sm-2 col-form-label">nama_barang</label>
                            <div class="col-sm-10">
                                <input type="text" class="form-control" id="nama_barang" name="Nama_barang" required>
                            </div>
                        </div>
                    </div>
                    <div class="card-footer">
                        <button type="submit" class="btn btn-primary">Save</button>
                    </div>
                </div>
            </form>
        </div>
        <form id="form-edit">
            <input type="hidden" id="id" name="id" value="">
            <div class="card">
                <div class="card-header">
                    <h2>Form Barang Edit</h2>
                </div>

                <div class="card-body">

                    <div class="row mb-3">
                        <label for="id_barang" class="col-sm-2 col-form-label">id_barang</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" id="id_barangedit" name="ID_barang" required>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <label for="nama_barang" class="col-sm-2 col-form-label">nama_barang</label>
                        <div class="col-sm-10">
                            <input type="text" class="form-control" id="nama_barangedit" name="Nama_barang" required>
                        </div>
                    </div>
                </div>
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary">Edit</button>
                </div>
            </div>
        </form>
    </div>
</body>
<script>
    var params = {
        display: 3,
        page: 1
    }
    const readData = () => {
        $.ajax({
            method: "GET",
            url: "http://localhost:3000/barang",
            dataType: "json",
            headers: {
                json: true
            },
            data: params
        }).done(function (data) {
            let html = ''
            data.forEach((item, index) => {
                html += `
    <tr>
      <td>${item.id_barang}</td>
      <td>${item.nama_barang}</td>
      <td>
        <button type="button" class="btn btn-success btn-sm btn-edit" id_data="${item.id_barang}"><i class="fa-solid fa-pencil"></i></button>
        <button type="button" class="btn btn-danger btn-sm btn-delete" id_data="${item.id_barang}"><i class="fa-solid fa-trash-can"></i></button>
      </td>
    </tr>
    `
            });
            $('table tbody').html(html)
        }).fail(function (err) {
            alert('gagal jquery')
        })
    }

    const saveData = (id_barang, nama_barang) => {
        $.ajax({
            method: "POST",
            url: `http://localhost:3000/barang`,
            dataType: "json",
            headers: {
                json: true
            },
            data: { id_barang, nama_barang }
        }).done(function (response) {
            readData()
            $("#form-tambah").trigger("reset");
        }).fail(function (err) {
            alert('gagal jquery')
        })
    }


    const saveEdit = (id_barang, nama_barang) => {
        $.ajax({
            method: "PUT",
            url: `http://localhost:3000/barang/${id_barang}`,
            dataType: "json",
            headers: {
                json: true
            },
            data: { nama_barang }
        }).done(function (response) {
            readData()
            $('#id_barang').val('')
            $("#form-edit").trigger("reset");
        }).fail(function (err) {
            alert('gagal pake jquery')
        })
    }


    const editData = (id) => {
        $.ajax({
            method: "GET",
            url: `http://localhost:3000/barang/${id}`,
            dataType: "json",
            headers: {
                json: true
            },
        }).done(function (data) {
            $('#id_barangedit').val(data[0].id_barang)
            $('#nama_barangedit').val(data[0].nama_barang)
        }).fail(function (err) {
            alert('gagal pake jquery')
        })
    }

    const deleteData = (id) => {
        $.ajax({
            method: "DELETE",
            url: `http://localhost:3000/barang/${id}`,
            dataType: "json",
            headers: {
                json: true
            },
        }).done(function (response) {
            readData()
        }).fail(function (err) {
            alert('gagal pake jquery')
        })
    }

    $(document).ready(function () {
        readData()

        $('#form-tambah').submit(function (event) {
            event.preventDefault();
            const id_barang = $('#id_barang').val()
            const nama_barang = $('#nama_barang').val()

            saveData(id_barang, nama_barang)
        })

        $('#form-edit').submit(function (event) {
            event.preventDefault();
            const id_barang = $('#id_barangedit').val()
            const nama_barang = $('#nama_barangedit').val()

            saveEdit(id_barang, nama_barang)
        })


        $('table tbody').on('click', '.btn-edit', function () {
            const id = $(this).attr('id_data')
            console.log(id)
            editData(id)
        })

        $('table tbody').on('click', '.btn-delete', function () {
            const id = $(this).attr('id_data')
            deleteData(id)
        })


    })

</script>
</body>

</html>